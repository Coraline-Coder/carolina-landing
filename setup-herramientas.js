const fs = require('fs');
const path = require('path');
const BASE = 'C:\\Users\\ACER\\Downloads\\carolina-landing';
const calcFile = path.join(BASE, 'calculadora-desperdicio.html');
const scoreFile = path.join(BASE, 'scorecard-meta-ads.html');
const benchFile = path.join(BASE, 'benchmark-meta-ads.html');
if (!fs.existsSync(calcFile) || !fs.existsSync(scoreFile) || !fs.existsSync(benchFile)) {
  console.log('Buscando HTMLs en upload...');
}
const uploadDir = path.join(BASE, '..', 'upload');
const calcHTML = fs.existsSync(calcFile) ? fs.readFileSync(calcFile, 'utf8') : (fs.existsSync(path.join(uploadDir, 'calculadora-desperdicio.html')) ? fs.readFileSync(path.join(uploadDir, 'calculadora-desperdicio.html'), 'utf8') : '');
const scoreHTML = fs.existsSync(scoreFile) ? fs.readFileSync(scoreFile, 'utf8') : (fs.existsSync(path.join(uploadDir, 'scorecard-meta-ads.html')) ? fs.readFileSync(path.join(uploadDir, 'scorecard-meta-ads.html'), 'utf8') : '');
const benchHTML = fs.existsSync(benchFile) ? fs.readFileSync(benchFile, 'utf8') : (fs.existsSync(path.join(uploadDir, 'benchmark-meta-ads.html')) ? fs.readFileSync(path.join(uploadDir, 'benchmark-meta-ads.html'), 'utf8') : '');
if (!calcHTML || !scoreHTML || !benchHTML) { console.error('ERROR: No se encontraron los 3 HTML. Ponlos en: ' + BASE); process.exit(1); }
function esc(s) { return s.replace(/\\/g, '\\\\').replace(/`/g, '\\`').replace(/\$/g, '\\$'); }
const comp = '"use client";\nimport React,{useState,useRef,useEffect,useCallback} from "react";\nimport{motion,AnimatePresence} from "framer-motion";\nconst NAVY="#0A0F1E";const BLUE="#4A7CF7";const WHITE="#FFFFFF";\nfunction ScrollReveal({children,className=""}:{children:React.ReactNode;className?:string}){\nconst ref=useRef<HTMLDivElement>(null);const[vis,setVis]=useState(false);\nuseEffect(()=>{const el=ref.current;if(!el)return;const obs=new IntersectionObserver(([e])=>{if(e.isIntersecting){setVis(true);obs.unobserve(el)}},{threshold:0.15});obs.observe(el);return()=>obs.disconnect()},[]);\nreturn(<motion.div ref={ref} initial={{opacity:0,y:32}} animate={vis?{opacity:1,y:0}:{}} transition={{duration:0.7,ease:"easeOut"}} className={className}>{children}</motion.div>);\n}\nconst calculadoraHTML=`'+esc(calcHTML)+'`;\nconst scorecardHTML=`'+esc(scoreHTML)+'`;\nconst benchmarkHTML=`'+esc(benchHTML)+'`;\nconst tools=[\n{id:"calculadora",emoji:"\\u{1F9CA}",label:"No se si mis ads funcionan",title:"Calculadora de desperdicio",desc:"Calcula en 30 segundos cuanto dinero se va sin generar ventas reales.",html:calculadoraHTML},\n{id:"scorecard",emoji:"\\u{1F321}\\uFE0F",label:"Invierto pero quiero escalar",title:"Scorecard de madurez",desc:"6 preguntas para saber exactamente que esta frenando el crecimiento de tus campanas.",html:scorecardHTML},\n{id:"benchmark",emoji:"\\u{1F525}",label:"Ya invierto fuerte y quiero comparar",title:"Benchmark de performance",desc:"Compara tus metricas con el promedio de tu industria y detecta las fugas.",html:benchmarkHTML},\n];\nfunction ToolIframe({html,onHeight}:{html:string;onHeight:(h:number)=>void}){\nconst iframeRef=useRef<HTMLIFrameElement>(null);\nconst handleLoad=useCallback(()=>{try{const doc=iframeRef.current?.contentDocument;if(doc?.body)onHeight(doc.body.scrollHeight)}catch{onHeight(900)}},[onHeight]);\nuseEffect(()=>{const handler=(e:MessageEvent)=>{if(e.data?.type==="resize"&&typeof e.data.height==="number")onHeight(e.data.height)};window.addEventListener("message",handler);return()=>window.removeEventListener("message",handler)},[onHeight]);\nreturn<iframe ref={iframeRef} srcDoc={html} onLoad={handleLoad} style={{width:"100%",border:"none",display:"block",minHeight:500,borderRadius:12}} sandbox="allow-scripts allow-popups allow-popups-to-escape-sandbox"/>;\n}\nexport default function HerramientasGratisSection(){\nconst[openTool,setOpenTool]=useState<string|null>(null);\nconst[iframeHeights,setIframeHeights]=useState<Record<string,number>>({});\nconst handleHeight=useCallback((toolId:string,h:number)=>{setIframeHeights((prev)=>({...prev,[toolId]:h}))},[]);\nreturn(\n<section style={{background:WHITE,padding:"6rem 2rem"}}>\n<div style={{maxWidth:1100,margin:"0 auto"}}>\n<ScrollReveal>\n<p style={{fontFamily:"var(--font-jost)",fontWeight:400,fontSize:"0.8rem",letterSpacing:"0.18em",textTransform:"uppercase" as const,color:BLUE,marginBottom:"0.75rem"}}>Herramientas gratuitas</p>\n<h2 style={{fontFamily:"var(--font-cormorant)",fontWeight:400,fontSize:"clamp(1.8rem,4vw,2.8rem)",color:NAVY,lineHeight:1.15,marginBottom:"0.5rem"}}>Empieza aqui. Gratis.</h2>\n<p style={{fontFamily:"var(--font-jost)",fontWeight:300,fontSize:"1.05rem",color:"#4A4A4A",lineHeight:1.7,maxWidth:600,marginBottom:"3rem"}}>Tres herramientas para saber exactamente donde esta tu problema — antes de hablar con alguien.</p>\n</ScrollReveal>\n<div className="herramientas-grid" style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:"1.5rem"}}>\n{tools.map((t)=>{const isOpen=openTool===t.id;return(\n<ScrollReveal key={t.id}>\n<div style={{background:isOpen?"rgba(74,124,247,0.06)":NAVY,border:isOpen?"2px solid rgba(74,124,247,0.4)":"1px solid rgba(74,124,247,0.15)",borderRadius:16,padding:"2rem 1.5rem",display:"flex",flexDirection:"column",height:"100%",transition:"all 0.3s ease",cursor:"pointer"}} onClick={()=>setOpenTool(isOpen?null:t.id)}>\n<span style={{fontSize:"1.8rem",marginBottom:"0.75rem"}}>{t.emoji}</span>\n<p style={{fontFamily:"var(--font-jost)",fontWeight:400,fontSize:"0.75rem",letterSpacing:"0.1em",textTransform:"uppercase" as const,color:BLUE,marginBottom:"0.5rem"}}>{t.label}</p>\n<h3 style={{fontFamily:"var(--font-cormorant)",fontWeight:500,fontSize:"1.25rem",color:isOpen?NAVY:WHITE,marginBottom:"0.5rem",lineHeight:1.2}}>{t.title}</h3>\n<p style={{fontFamily:"var(--font-jost)",fontWeight:300,fontSize:"0.85rem",color:isOpen?"#4A4A4A":"rgba(255,255,255,0.6)",lineHeight:1.6,flex:1}}>{t.desc}</p>\n<button style={{fontFamily:"var(--font-jost)",fontWeight:500,fontSize:"0.8rem",color:WHITE,background:BLUE,border:"none",borderRadius:8,padding:"0.65rem 1.2rem",marginTop:"1.2rem",cursor:"pointer",letterSpacing:"0.06em"}} onClick={(e)=>{e.stopPropagation();setOpenTool(isOpen?null:t.id)}}>{isOpen?"Cerrar":"Usar herramienta"} →</button>\n</div>\n</ScrollReveal>);})}\n</div>\n<AnimatePresence>\n{openTool&&(\n<motion.div initial={{opacity:0,height:0}} animate={{opacity:1,height:"auto"}} exit={{opacity:0,height:0}} transition={{duration:0.4,ease:"easeInOut"}} style={{overflow:"hidden",marginTop:"2rem"}}>\n<div style={{background:"#F5F6FA",border:"1px solid #E2E6F0",borderRadius:16,padding:"1.5rem",maxWidth:720,margin:"0 auto"}}>\n<div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:"1rem"}}>\n<h3 style={{fontFamily:"var(--font-cormorant)",fontWeight:500,fontSize:"1.3rem",color:NAVY}}>{tools.find((t)=>t.id===openTool)?.emoji} {tools.find((t)=>t.id===openTool)?.title}</h3>\n<button onClick={()=>setOpenTool(null)} style={{background:"transparent",border:"1px solid #D0D5E8",borderRadius:8,padding:"0.4rem 1rem",fontFamily:"var(--font-jost)",fontSize:"0.75rem",color:"#8A8FA8",cursor:"pointer",letterSpacing:"0.06em",textTransform:"uppercase" as const}}>Cerrar ✕</button>\n</div>\n<ToolIframe key={openTool} html={tools.find((t)=>t.id===openTool)!.html} onHeight={(h)=>handleHeight(openTool,h)}/>\n</div>\n</motion.div>)}\n</AnimatePresence>\n</div>\n</section>);\n}\n';
fs.writeFileSync(path.join(BASE,'src','components','herramientas-gratis.tsx'),comp,'utf8');
console.log('herramientas-gratis.tsx creado OK');
let page=fs.readFileSync(path.join(BASE,'src','app','page.tsx'),'utf8').replace(/\r\n/g,'\n');
if(!page.includes('HerramientasGratisSection')){page=page.replace('import CalculadoraROAS from "@/components/CalculadoraROAS";','import CalculadoraROAS from "@/components/CalculadoraROAS";\nimport HerramientasGratisSection from "@/components/herramientas-gratis";');}
if(!page.includes('<HerramientasGratisSection />')){page=page.replace('<CasoDeExitoSection />\n      <SistemaFiltroSection />','<CasoDeExitoSection />\n      <HerramientasGratisSection />\n      <SistemaFiltroSection />');}
fs.writeFileSync(path.join(BASE,'src','app','page.tsx'),page,'utf8');
console.log('page.tsx parcheado OK');
const css='\n.herramientas-grid{display:grid}\n@media(max-width:768px){.herramientas-grid{grid-template-columns:1fr !important;gap:1rem !important}}\n';
let g=fs.readFileSync(path.join(BASE,'src','app','globals.css'),'utf8');
if(!g.includes('herramientas-grid')){fs.appendFileSync(path.join(BASE,'src','app','globals.css'),css,'utf8');console.log('globals.css parcheado OK');}
console.log('TODO LISTO! Ahora haz git add . && git commit -m "feat: 3 herramientas interactivas" && git push');
